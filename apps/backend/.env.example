# Backend-local overrides. The root .env is loaded as a fallback (see
# app.module.ts envFilePath), so for normal dev you only need the root one.

DATABASE_URL="postgresql://expense:expense@localhost:5432/expense_tracker?schema=public"
PORT=3001
CORS_ORIGIN=http://localhost:3000
